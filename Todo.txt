Jobs:
- If performance becomes an issue for whatever reason, barriers might be better
  than busy looping + yielding
  http://en.cppreference.com/w/cpp/experimental/barrier
- Stop idle threads from pegging the cpu (see solution above)
